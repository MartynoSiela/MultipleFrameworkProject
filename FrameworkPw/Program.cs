namespace FrameworkPw
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            /*
             * This method is needed just because Playwright
             * seems to not work in Class Library type project
             * And Console App type project needs a Main method
             * to exist.
             */
        }
    }
}
